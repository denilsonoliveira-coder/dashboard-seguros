"use client";
import{Bar,BarChart,CartesianGrid,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';import{abbr,integer}from'@/lib/data';const colors=['#0A2D87','#2871d4','#12a66a','#f59e0b','#7c3aed'];
export default function MonthlyChart({data,series,horizontal=false}:{data:Record<string,string|number>[];series:string[];horizontal?:boolean}){return <div className="h-[430px]"><ResponsiveContainer><BarChart data={data} layout={horizontal?'vertical':'horizontal'} margin={{right:15}}><CartesianGrid stroke="#e8edf5" strokeDasharray="3 3" vertical={!horizontal} horizontal={horizontal}/>{horizontal?<><XAxis type="number" tickFormatter={abbr}/><YAxis type="category" dataKey="mes" width={45}/></>:<><XAxis dataKey="mes"/><YAxis tickFormatter={abbr}/></>}<Tooltip formatter={v=>integer(Number(v))}/><Legend/>{series.map((s,i)=><Bar key={s} dataKey={s} fill={colors[i%colors.length]} radius={horizontal?[0,5,5,0]:[5,5,0,0]} maxBarSize={24}/>)}</BarChart></ResponsiveContainer></div>}