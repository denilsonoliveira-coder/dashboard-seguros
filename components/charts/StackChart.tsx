"use client";import{Bar,BarChart,CartesianGrid,LabelList,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';import{abbr,money}from'@/lib/data';const C=['#0A2D87','#2871d4','#12a66a','#f59e0b'];export default function StackChart({data,series}:{data:Record<string,string|number>[];series:string[]}){return <div className="h-[430px]"><ResponsiveContainer><BarChart data={data}><CartesianGrid stroke="#e8edf5" strokeDasharray="3 5" vertical={false}/><XAxis dataKey="mes"/><YAxis tickFormatter={abbr}/><Tooltip formatter={v=>money(Number(v))}/><Legend iconType="circle"/>{series.map((s,i)=><Bar key={s} dataKey={s} stackId="a" fill={C[i%C.length]} radius={[8,8,8,8]}><LabelList dataKey={s} position="center" formatter={(v:number)=>v?abbr(v):''} fill="white" fontSize={10}/></Bar>)}</BarChart></ResponsiveContainer></div>}