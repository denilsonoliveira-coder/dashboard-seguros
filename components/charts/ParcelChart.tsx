"use client";
import{Bar,BarChart,CartesianGrid,LabelList,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';import{abbr,money}from'@/lib/data';const colors=['#0A2D87','#2871d4','#12a66a','#f59e0b','#7c3aed'];
export default function ParcelChart({data,series}:{data:Record<string,string|number>[];series:string[]}){return <div className="h-[430px]"><ResponsiveContainer><BarChart data={data} margin={{top:20}}><CartesianGrid stroke="#e8edf5" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="mes"/><YAxis tickFormatter={abbr}/><Tooltip formatter={v=>money(Number(v))}/><Legend/>{series.map((s,i)=><Bar key={s} dataKey={s} stackId="a" fill={colors[i%colors.length]}><LabelList dataKey={s} position="center" formatter={(v:number)=>v?abbr(v):''} fill="white" fontSize={10}/></Bar>)}</BarChart></ResponsiveContainer></div>}